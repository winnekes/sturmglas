query Statistics {
  statistics {
    moodCounts {
      count
      emotion
    }
    tagUsageCounts {
      tag
      count
    }
  }
  profile {
    id
    nickname
    pictureUrl
    refreshToken
    settings
    longestStreak
    currentStreak
  }
}
